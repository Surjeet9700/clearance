// Load the MCP-B polyfill so agent tools register in ANY browser (Chrome stable,
// ChatGPT desktop in-app browser, Safari). Native modelContext takes precedence when present.
import '@mcp-b/global'
